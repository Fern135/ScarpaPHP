window.onload = Load();

async function Load(){
    try {
        Promise.all(
            [

            ]
        );
    } catch (error) {
        console.error(`Error: ${error.toString()}`);
    }
}

